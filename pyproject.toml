[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nderf_xtract"
version = "0.1"
description = "NDERF Toolkit"
authors = [{name = "Jacob"}]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]